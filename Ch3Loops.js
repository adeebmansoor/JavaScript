// for Loop
// Program for factorial of a n natural numbers?
// const prompt = require("prompt-sync")();
// let fact = 1;
// let n = prompt("Enter the value of n: ");
// n = Number.parseInt(n);
// for(let i=1; i<=n; i++){
//     fact *= i;
// }
// console.log("Factorial of " + n + " is " + fact);

// for-in Loop (it loops through the keys of an object)
/* let obj = {
    A : '76',
    B : '90',
    C : '56',
    D : '32',
}
for (let a in obj){
    console.log("Marks of " + a + " are " + obj[a]);
} */

// For-of Loop (it loops through the values of an object)
/* for (let b of "Adeeb Mansoor"){
    console.log(b);
} */

// while Loop
// Program for sum of n natural numbers?
/* const prompt = require("prompt-sync")();
let n = prompt("Enter the value of n: ");
n = Number.parseInt(n);
let sum = 0;
let i = 0;
while(i<=n){
    sum += i;
    i++;
}
console.log(sum); */

// do-while Loop
// Multiplication table of nth number?
// const prompt = require("prompt-sync")();
// let n = prompt("Enter the value of n: ");
// n = Number.parseInt(n);
// let i = 1;
// do{
//     console.log(n + " X " + i + " = " + n*i);
//     i++;
// }while(i<=10)